%p= I18n.t('devise.email_changed.greeting', email: @email)
- if @resource.try(:unconfirmed_email?)
  %p= I18n.t('devise.email_changed.unconfirmed_email_true', email: @resource.unconfirmed_email)
- else
  %p= I18n.t('devise.email_changed.unconfirmed_email_false', email: @resource.email)
