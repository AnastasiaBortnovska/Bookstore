%p= t('.hello', email: @resource.email)
%p= t('.info_for_change_password')
%p= link_to t('.link_change_password'), edit_password_url(@resource, reset_password_token: @token)
%p= t('.info_if_wont_change')
