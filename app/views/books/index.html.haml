%main.container
  = render 'books/partials/filter', cached: true
  .mb-120
    .row#show-books
      = render partial: 'books/partials/book', collection: books, as: :book
    - if (@pagy.next)
      .text-center#more-books
        = render 'books/partials/next_page_link', pagy: @pagy
